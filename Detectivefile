# find the ruby interperter's version

investigate! :ruby_tuesday do
  unless_its_true_that? "ruby -v | grep 'ruby 1'" do
    you_need :apt => "pkg=ruby1.8 state=latest"
  end

  if_its_true_that? "ruby -v | grep 'ruby 1'" do
    you_need :apt => "pkg=ruby1.8 state=absent"
  end
end
